/**
 * Crossly Seller API
 *
 * The seller-facing Crossly API — listings, crossposting, orders, analytics. Authenticate with a Personal Access Token (`Authorization: Bearer crossly_pat_…`) or a seller OAuth token (`crossly_oat_…`), scoped `<domain>:<read|write>`. Buyer endpoints are a SEPARATE API with a separate principal — see the Crossly Buyer API.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package net.crossly.models

import net.crossly.models.CreateMagicScanSynthesizeResponsePayloadCategory
import net.crossly.models.CreateMagicScanSynthesizeResponsePayloadChosenDimensions
import net.crossly.models.CreateMagicScanSynthesizeResponsePayloadDimensions
import net.crossly.models.CreateMagicScanSynthesizeResponsePayloadGrading
import net.crossly.models.CreateMagicScanSynthesizeResponsePayloadSectionApplicability

import com.squareup.moshi.Json

/**
 * 
 *
 * @param title 
 * @param description 
 * @param perPlatformOverrides Per-platform required-fields map — slotted directly into  platformOverrides when the form hydrates.
 * @param confidence AI's confidence in the synthesis (0-1). Surfaced to the seller  so they can decide whether to skim or trust + click.
 * @param brand 
 * @param grading 
 * @param color 
 * @param material 
 * @param propertySize 
 * @param sizeSystem Poshmark/Vestiaire size system — \"US\", \"EU\", \"UK\", etc.
 * @param weightOz 
 * @param dimensions 
 * @param priceCents Echoed user-selected price (cents). Set on every synthesize call  so the master form's defaultPrice input gets populated regardless  of which path (AI or heuristic) generated the rest.
 * @param sellerNote The seller's own words about this item — the pre-scan hint, or the  post-scan note that supersedes it (see synthesize/seller-note.ts).  Carried on the payload so the TAXONOMY resolvers see it too: category  and facet picking happen in their own LLM calls, which never saw the  note even after the main synthesis prompt did. A seller correcting the  variant was still getting the category of the wrong one.
 * @param priceLowCents Comp price band (cents), outlier-trimmed — low/median/high across the  matched comps. The UI surfaces the range so the seller prices  strategically instead of trusting one number; priceCents defaults to  the median when the seller hasn't picked.
 * @param priceMedianCents 
 * @param priceHighCents 
 * @param quantity Echoed seller-supplied listable-unit count from Magic List. This is  the listing's ADVERTISED stock (how many of this listing to sell).  Hydrates the form's `quantity` field. Undefined = leave form default (1).
 * @param inventoryQuantity Physical units the seller actually has in stock — becomes the auto-  created inventory item's quantity/quantityAvailable. Distinct from  `quantity` (advertised stock) and `unitsPerListing` (composition).  Undefined = fall back to `quantity`.
 * @param unitsPerListing How many physical units are bundled inside ONE listing (composition;  e.g. a pack of 4). Becomes listing_inventory_items.quantity so the  multi-channel fulfillable = floor(inventoryQuantity / unitsPerListing).  Undefined = 1.
 * @param costBasisCents What the seller paid, per physical unit, in cents — the auto-created  inventory item's `costBasisCents`. Nothing wrote that column from any  create path, so every Magic List item had a NULL cost basis and every  sale of one reported its full sale price as profit. Undefined = unknown,  which stays NULL (0 would be a claim that the item was free).
 * @param chosenDimensions 
 * @param imageUrls Every photo the seller uploaded for the source scan, primary  first. The NewListingPage hydrator drops these straight into the  form's images state so the seller doesn't have to re-upload.
 * @param category 
 * @param department eBay item-specifics: Department/Gender/Style/Pattern/Type. These  drive the eBay-required aspects on the form's Item Details  section. Mostly only eBay matches have them.
 * @param gender 
 * @param style 
 * @param pattern 
 * @param itemType 
 * @param upc Universal product code, when eBay's enriched detail surfaced it.  Hydrates the master form's UPC/GTIN field.
 * @param handlingTimeDays 
 * @param returnWindowDays 
 * @param tags 
 * @param itemSpecifics Master-level item specifics (JSON Record<string,string[]>). Item  specifics are now master-owned + fanned to every aspect platform, so the  form's shared Item-specifics editor prefills from this. Mirrored from the  richest resolved per-platform specifics blob (eBay after full-aspect  enrichment). Undefined = no specifics.
 * @param notes AI's free-text rationale for the merge — what it pulled from  where. Helps the seller spot a bad merge before listing.
 * @param sectionApplicability 
 */

data class CreateMagicScanSynthesizeResponsePayload (

    @Json(name = "title")
    val title: kotlin.String,

    @Json(name = "description")
    val description: kotlin.String,

    /* Per-platform required-fields map — slotted directly into  platformOverrides when the form hydrates. */
    @Json(name = "perPlatformOverrides")
    val perPlatformOverrides: kotlin.Any,

    /* AI's confidence in the synthesis (0-1). Surfaced to the seller  so they can decide whether to skim or trust + click. */
    @Json(name = "confidence")
    val confidence: java.math.BigDecimal,

    @Json(name = "brand")
    val brand: kotlin.String? = null,

    @Json(name = "grading")
    val grading: CreateMagicScanSynthesizeResponsePayloadGrading? = null,

    @Json(name = "color")
    val color: kotlin.String? = null,

    @Json(name = "material")
    val material: kotlin.String? = null,

    @Json(name = "size")
    val propertySize: kotlin.String? = null,

    /* Poshmark/Vestiaire size system — \"US\", \"EU\", \"UK\", etc. */
    @Json(name = "sizeSystem")
    val sizeSystem: kotlin.String? = null,

    @Json(name = "weightOz")
    val weightOz: java.math.BigDecimal? = null,

    @Json(name = "dimensions")
    val dimensions: CreateMagicScanSynthesizeResponsePayloadDimensions? = null,

    /* Echoed user-selected price (cents). Set on every synthesize call  so the master form's defaultPrice input gets populated regardless  of which path (AI or heuristic) generated the rest. */
    @Json(name = "priceCents")
    val priceCents: java.math.BigDecimal? = null,

    /* The seller's own words about this item — the pre-scan hint, or the  post-scan note that supersedes it (see synthesize/seller-note.ts).  Carried on the payload so the TAXONOMY resolvers see it too: category  and facet picking happen in their own LLM calls, which never saw the  note even after the main synthesis prompt did. A seller correcting the  variant was still getting the category of the wrong one. */
    @Json(name = "sellerNote")
    val sellerNote: kotlin.String? = null,

    /* Comp price band (cents), outlier-trimmed — low/median/high across the  matched comps. The UI surfaces the range so the seller prices  strategically instead of trusting one number; priceCents defaults to  the median when the seller hasn't picked. */
    @Json(name = "priceLowCents")
    val priceLowCents: java.math.BigDecimal? = null,

    @Json(name = "priceMedianCents")
    val priceMedianCents: java.math.BigDecimal? = null,

    @Json(name = "priceHighCents")
    val priceHighCents: java.math.BigDecimal? = null,

    /* Echoed seller-supplied listable-unit count from Magic List. This is  the listing's ADVERTISED stock (how many of this listing to sell).  Hydrates the form's `quantity` field. Undefined = leave form default (1). */
    @Json(name = "quantity")
    val quantity: java.math.BigDecimal? = null,

    /* Physical units the seller actually has in stock — becomes the auto-  created inventory item's quantity/quantityAvailable. Distinct from  `quantity` (advertised stock) and `unitsPerListing` (composition).  Undefined = fall back to `quantity`. */
    @Json(name = "inventoryQuantity")
    val inventoryQuantity: java.math.BigDecimal? = null,

    /* How many physical units are bundled inside ONE listing (composition;  e.g. a pack of 4). Becomes listing_inventory_items.quantity so the  multi-channel fulfillable = floor(inventoryQuantity / unitsPerListing).  Undefined = 1. */
    @Json(name = "unitsPerListing")
    val unitsPerListing: java.math.BigDecimal? = null,

    /* What the seller paid, per physical unit, in cents — the auto-created  inventory item's `costBasisCents`. Nothing wrote that column from any  create path, so every Magic List item had a NULL cost basis and every  sale of one reported its full sale price as profit. Undefined = unknown,  which stays NULL (0 would be a claim that the item was free). */
    @Json(name = "costBasisCents")
    val costBasisCents: java.math.BigDecimal? = null,

    @Json(name = "chosenDimensions")
    val chosenDimensions: CreateMagicScanSynthesizeResponsePayloadChosenDimensions? = null,

    /* Every photo the seller uploaded for the source scan, primary  first. The NewListingPage hydrator drops these straight into the  form's images state so the seller doesn't have to re-upload. */
    @Json(name = "imageUrls")
    val imageUrls: kotlin.collections.List<kotlin.String>? = null,

    @Json(name = "category")
    val category: CreateMagicScanSynthesizeResponsePayloadCategory? = null,

    /* eBay item-specifics: Department/Gender/Style/Pattern/Type. These  drive the eBay-required aspects on the form's Item Details  section. Mostly only eBay matches have them. */
    @Json(name = "department")
    val department: kotlin.String? = null,

    @Json(name = "gender")
    val gender: kotlin.String? = null,

    @Json(name = "style")
    val style: kotlin.String? = null,

    @Json(name = "pattern")
    val pattern: kotlin.String? = null,

    @Json(name = "itemType")
    val itemType: kotlin.String? = null,

    /* Universal product code, when eBay's enriched detail surfaced it.  Hydrates the master form's UPC/GTIN field. */
    @Json(name = "upc")
    val upc: kotlin.String? = null,

    @Json(name = "handlingTimeDays")
    val handlingTimeDays: kotlin.String? = null,

    @Json(name = "returnWindowDays")
    val returnWindowDays: kotlin.String? = null,

    @Json(name = "tags")
    val tags: kotlin.collections.List<kotlin.String>? = null,

    /* Master-level item specifics (JSON Record<string,string[]>). Item  specifics are now master-owned + fanned to every aspect platform, so the  form's shared Item-specifics editor prefills from this. Mirrored from the  richest resolved per-platform specifics blob (eBay after full-aspect  enrichment). Undefined = no specifics. */
    @Json(name = "itemSpecifics")
    val itemSpecifics: kotlin.String? = null,

    /* AI's free-text rationale for the merge — what it pulled from  where. Helps the seller spot a bad merge before listing. */
    @Json(name = "notes")
    val notes: kotlin.String? = null,

    @Json(name = "sectionApplicability")
    val sectionApplicability: CreateMagicScanSynthesizeResponsePayloadSectionApplicability? = null

)

