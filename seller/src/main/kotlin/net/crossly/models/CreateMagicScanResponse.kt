/**
 * Crossly Seller API
 *
 * The seller-facing Crossly API — listings, crossposting, orders, analytics. Authenticate with a Personal Access Token (`Authorization: Bearer crossly_pat_…`) or a seller OAuth token (`crossly_oat_…`), scoped `<domain>:<read|write>`. Buyer endpoints are a SEPARATE API with a separate principal — see the Crossly Buyer API.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package net.crossly.models

import net.crossly.models.CreateMagicScanResponseEbayHits
import net.crossly.models.CreateMagicScanResponseEbayMatch
import net.crossly.models.CreateMagicScanResponsePossibleDuplicates
import net.crossly.models.CreateMagicScanResponseTopHits

import com.squareup.moshi.Json

/**
 * 
 *
 * @param runId 
 * @param topHits Unified top-10-globally list, ranked by CLIP visual similarity to  the seller's source photo. Each hit carries its origin platform.
 * @param ebayHits Legacy compat — UI's existing render. ebayHits now == visually-  validated eBay subset; otherMatches is re-grouped from topHits.
 * @param otherMatches 
 * @param imageUrls Every photo the seller uploaded for this scan, primary first.
 * @param visionAspects Vision-LLM aspects extracted across all photos. Populated only  when the seller has magic-list-vision-aspects enabled + a vision  provider configured. Empty otherwise.
 * @param possibleDuplicates The seller's OWN listings/inventory that this scan probably duplicates  (image + fuzzy-title self-dedup). Empty when nothing matched. Drives the  \"you may already have this\" prompt.
 * @param cached 
 * @param ebayMatch 
 */

data class CreateMagicScanResponse (

    @Json(name = "runId")
    val runId: kotlin.String,

    /* Unified top-10-globally list, ranked by CLIP visual similarity to  the seller's source photo. Each hit carries its origin platform. */
    @Json(name = "topHits")
    val topHits: kotlin.collections.List<CreateMagicScanResponseTopHits>,

    /* Legacy compat — UI's existing render. ebayHits now == visually-  validated eBay subset; otherMatches is re-grouped from topHits. */
    @Json(name = "ebayHits")
    val ebayHits: kotlin.collections.List<CreateMagicScanResponseEbayHits>,

    @Json(name = "otherMatches")
    val otherMatches: kotlin.Any,

    /* Every photo the seller uploaded for this scan, primary first. */
    @Json(name = "imageUrls")
    val imageUrls: kotlin.collections.List<kotlin.String>,

    /* Vision-LLM aspects extracted across all photos. Populated only  when the seller has magic-list-vision-aspects enabled + a vision  provider configured. Empty otherwise. */
    @Json(name = "visionAspects")
    val visionAspects: kotlin.Any,

    /* The seller's OWN listings/inventory that this scan probably duplicates  (image + fuzzy-title self-dedup). Empty when nothing matched. Drives the  \"you may already have this\" prompt. */
    @Json(name = "possibleDuplicates")
    val possibleDuplicates: kotlin.collections.List<CreateMagicScanResponsePossibleDuplicates>,

    @Json(name = "cached")
    val cached: kotlin.Boolean,

    @Json(name = "ebayMatch")
    val ebayMatch: CreateMagicScanResponseEbayMatch? = null

)

