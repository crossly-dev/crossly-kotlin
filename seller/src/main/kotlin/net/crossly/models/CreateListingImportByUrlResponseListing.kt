/**
 * Crossly Seller API
 *
 * The seller-facing Crossly API — listings, crossposting, orders, analytics. Authenticate with a Personal Access Token (`Authorization: Bearer crossly_pat_…`) or a seller OAuth token (`crossly_oat_…`), scoped `<domain>:<read|write>`. Buyer endpoints are a SEPARATE API with a separate principal — see the Crossly Buyer API.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package net.crossly.models


import com.squareup.moshi.Json

/**
 * 
 *
 * @param platformListingId 
 * @param title 
 * @param description 
 * @param price 
 * @param images 
 * @param brand 
 * @param condition 
 * @param propertySize 
 * @param color Color(s) the platform's own response exposes (e.g. Poshmark's  `colors` array). Only set when actually present in the scrape.
 * @param sku 
 * @param url 
 * @param category The platform's OWN category, as a top-down path (e.g. Poshmark's  [\"Kids\", \"Toys\", \"Dolls & Accessories\"] from department → category →  feature). Only set when the platform's list/scrape response actually  carries this — never guessed. Mapped onto Crossly's category.main/  sub/sub2 (listings) or categoryMain/categorySub (inventory_items) at  create/enrich time.
 * @param categoryId The platform's raw numeric category id, same space as  overrides.<platform>.categoryId — distinct from `category`'s  human-readable path. Only set by platforms whose id space is directly  comparable to what we publish (currently eBay's drift-check second  call — see diff-fields/second-call.ts's ebayRemoteFields). eBay's own  CategoryName wording/depth is a different vocabulary from Crossly's  master taxonomy breadcrumb and will essentially never string-match  it, so drift-detection compares ids instead of names.
 * @param tags Tag-like strings the platform's own response exposes (e.g. Poshmark's  marketing \"experience\" tags). Distinct from a full search-tag  generator — just whatever real tag data the scrape already carries.
 * @param material Structured item aspects — the same shape the listing form's Item  Details section captures (and templates already persist), now sourced  from the platform's own data instead of only manual entry. Only set  when the platform's response genuinely carries a semantically-matching  field (eBay item specifics, Facebook attributes, Poshmark catalog,  Depop's detail-call ride-along) — never derived or guessed.
 * @param style 
 * @param pattern 
 * @param department 
 * @param gender 
 * @param itemType 
 * @param sizeSystem 
 * @param itemSpecifics Category-specific facets from a per-item DETAIL call (e.g. Facebook's  Age Range/Character/Age Group), keyed the same way eBay item-specifics  are: aspect name -> value array. Only set by platforms with a genuine  per-item attribute source — costs one extra call per listing, so  populated by a dedicated enrichment pass, not the main list mapper.  Maps onto listings.itemSpecifics; inventory_items has no equivalent  column.
 * @param listedAt When the listing was first published on the platform. Used by  the Advanced filter's listedAfter / listedBefore knobs. Optional  because not every platform returns it on the listing endpoint.
 * @param quantity Units this platform reports. Only meaningful for platforms  `PLATFORM_QUANTITY_SYNC` marks 'native' — relist platforms show one item  and say 1 forever, so they leave this undefined rather than voting with  a number they cannot actually express. See `_quantity.ts`.
 * @param accountSlot Which of the user's connected accounts on this platform this listing  was scraped from — stamped by fetchCookieListings as it loops each  connected account (see listActiveForPlatform in  user-platform-accounts/read.ts). Undefined for API-track platforms  (single connection, no multi-account concept) and for any cookie path  that hasn't been threaded yet; importOne treats undefined as slot 1,  matching the historical single-account default.
 * @param weightOz Total item weight in ounces (already summed, not split lb+oz —  the write path converts to the DB's weightLb+weightOz split).
 * @param dimensionLIn 
 * @param dimensionWIn 
 * @param dimensionHIn 
 * @param handlingTimeDays Max days the platform's own listing commits to ship within (eBay's  DispatchTimeMax, Etsy's processing_max).
 * @param bestOfferAutoAcceptCents 
 * @param bestOfferAutoDeclineCents 
 * @param itemLocation Free text as the platform itself expresses it (e.g. eBay's `Location`  is a single seller-typed string like \"Austin, TX\", not a structured  address) — never parsed into city/state.
 * @param itemLocationZip 
 * @param itemLocationCountry 
 * @param declaredShippingCost What the platform's OWN listing declares shipping costs — reference  only, distinct from a realized post-sale shipping cost.
 * @param returnPolicyText 
 */

data class CreateListingImportByUrlResponseListing (

    @Json(name = "platformListingId")
    val platformListingId: kotlin.String,

    @Json(name = "title")
    val title: kotlin.String,

    @Json(name = "description")
    val description: kotlin.String? = null,

    @Json(name = "price")
    val price: java.math.BigDecimal? = null,

    @Json(name = "images")
    val images: kotlin.collections.List<kotlin.String>? = null,

    @Json(name = "brand")
    val brand: kotlin.String? = null,

    @Json(name = "condition")
    val condition: kotlin.String? = null,

    @Json(name = "size")
    val propertySize: kotlin.String? = null,

    /* Color(s) the platform's own response exposes (e.g. Poshmark's  `colors` array). Only set when actually present in the scrape. */
    @Json(name = "color")
    val color: kotlin.collections.List<kotlin.String>? = null,

    @Json(name = "sku")
    val sku: kotlin.String? = null,

    @Json(name = "url")
    val url: kotlin.String? = null,

    /* The platform's OWN category, as a top-down path (e.g. Poshmark's  [\"Kids\", \"Toys\", \"Dolls & Accessories\"] from department → category →  feature). Only set when the platform's list/scrape response actually  carries this — never guessed. Mapped onto Crossly's category.main/  sub/sub2 (listings) or categoryMain/categorySub (inventory_items) at  create/enrich time. */
    @Json(name = "category")
    val category: kotlin.collections.List<kotlin.String>? = null,

    /* The platform's raw numeric category id, same space as  overrides.<platform>.categoryId — distinct from `category`'s  human-readable path. Only set by platforms whose id space is directly  comparable to what we publish (currently eBay's drift-check second  call — see diff-fields/second-call.ts's ebayRemoteFields). eBay's own  CategoryName wording/depth is a different vocabulary from Crossly's  master taxonomy breadcrumb and will essentially never string-match  it, so drift-detection compares ids instead of names. */
    @Json(name = "categoryId")
    val categoryId: kotlin.String? = null,

    /* Tag-like strings the platform's own response exposes (e.g. Poshmark's  marketing \"experience\" tags). Distinct from a full search-tag  generator — just whatever real tag data the scrape already carries. */
    @Json(name = "tags")
    val tags: kotlin.collections.List<kotlin.String>? = null,

    /* Structured item aspects — the same shape the listing form's Item  Details section captures (and templates already persist), now sourced  from the platform's own data instead of only manual entry. Only set  when the platform's response genuinely carries a semantically-matching  field (eBay item specifics, Facebook attributes, Poshmark catalog,  Depop's detail-call ride-along) — never derived or guessed. */
    @Json(name = "material")
    val material: kotlin.String? = null,

    @Json(name = "style")
    val style: kotlin.String? = null,

    @Json(name = "pattern")
    val pattern: kotlin.String? = null,

    @Json(name = "department")
    val department: kotlin.String? = null,

    @Json(name = "gender")
    val gender: kotlin.String? = null,

    @Json(name = "itemType")
    val itemType: kotlin.String? = null,

    @Json(name = "sizeSystem")
    val sizeSystem: kotlin.String? = null,

    /* Category-specific facets from a per-item DETAIL call (e.g. Facebook's  Age Range/Character/Age Group), keyed the same way eBay item-specifics  are: aspect name -> value array. Only set by platforms with a genuine  per-item attribute source — costs one extra call per listing, so  populated by a dedicated enrichment pass, not the main list mapper.  Maps onto listings.itemSpecifics; inventory_items has no equivalent  column. */
    @Json(name = "itemSpecifics")
    val itemSpecifics: kotlin.Any? = null,

    /* When the listing was first published on the platform. Used by  the Advanced filter's listedAfter / listedBefore knobs. Optional  because not every platform returns it on the listing endpoint. */
    @Json(name = "listedAt")
    val listedAt: java.time.OffsetDateTime? = null,

    /* Units this platform reports. Only meaningful for platforms  `PLATFORM_QUANTITY_SYNC` marks 'native' — relist platforms show one item  and say 1 forever, so they leave this undefined rather than voting with  a number they cannot actually express. See `_quantity.ts`. */
    @Json(name = "quantity")
    val quantity: java.math.BigDecimal? = null,

    /* Which of the user's connected accounts on this platform this listing  was scraped from — stamped by fetchCookieListings as it loops each  connected account (see listActiveForPlatform in  user-platform-accounts/read.ts). Undefined for API-track platforms  (single connection, no multi-account concept) and for any cookie path  that hasn't been threaded yet; importOne treats undefined as slot 1,  matching the historical single-account default. */
    @Json(name = "accountSlot")
    val accountSlot: java.math.BigDecimal? = null,

    /* Total item weight in ounces (already summed, not split lb+oz —  the write path converts to the DB's weightLb+weightOz split). */
    @Json(name = "weightOz")
    val weightOz: java.math.BigDecimal? = null,

    @Json(name = "dimensionLIn")
    val dimensionLIn: java.math.BigDecimal? = null,

    @Json(name = "dimensionWIn")
    val dimensionWIn: java.math.BigDecimal? = null,

    @Json(name = "dimensionHIn")
    val dimensionHIn: java.math.BigDecimal? = null,

    /* Max days the platform's own listing commits to ship within (eBay's  DispatchTimeMax, Etsy's processing_max). */
    @Json(name = "handlingTimeDays")
    val handlingTimeDays: java.math.BigDecimal? = null,

    @Json(name = "bestOfferAutoAcceptCents")
    val bestOfferAutoAcceptCents: java.math.BigDecimal? = null,

    @Json(name = "bestOfferAutoDeclineCents")
    val bestOfferAutoDeclineCents: java.math.BigDecimal? = null,

    /* Free text as the platform itself expresses it (e.g. eBay's `Location`  is a single seller-typed string like \"Austin, TX\", not a structured  address) — never parsed into city/state. */
    @Json(name = "itemLocation")
    val itemLocation: kotlin.String? = null,

    @Json(name = "itemLocationZip")
    val itemLocationZip: kotlin.String? = null,

    @Json(name = "itemLocationCountry")
    val itemLocationCountry: kotlin.String? = null,

    /* What the platform's OWN listing declares shipping costs — reference  only, distinct from a realized post-sale shipping cost. */
    @Json(name = "declaredShippingCost")
    val declaredShippingCost: java.math.BigDecimal? = null,

    @Json(name = "returnPolicyText")
    val returnPolicyText: kotlin.String? = null

)

