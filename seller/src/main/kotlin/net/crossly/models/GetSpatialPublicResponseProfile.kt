/**
 * Crossly Seller API
 *
 * The seller-facing Crossly API — listings, crossposting, orders, analytics. Authenticate with a Personal Access Token (`Authorization: Bearer crossly_pat_…`) or a seller OAuth token (`crossly_oat_…`), scoped `<domain>:<read|write>`. Buyer endpoints are a SEPARATE API with a separate principal — see the Crossly Buyer API.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package net.crossly.models

import net.crossly.models.GetSpatialPublicResponseProfileContainers
import net.crossly.models.GetSpatialPublicResponseProfileItemSize

import com.squareup.moshi.Json

/**
 * 
 *
 * @param categorySlug Matches `crossly_market_categories.slug`, or '*' for the fallback.
 * @param label Room title, shown in the switcher.
 * @param tagline One line of why this space is shaped the way it is. Surfaced in the UI.
 * @param presentation 
 * @param itemSize 
 * @param containers The container ladder, OUTERMOST FIRST. A binder holds pages, a page holds  cards. The solver walks this to decide what to create next when the  current container fills up.
 * @param defaultGroupBy Default grouping, in order. Each level becomes a divider or a container.
 * @param defaultSortBy Default ordering inside a group.
 * @param itemModelVariants Other meshes items in this category may be drawn as, chosen PER ITEM.    One model per category is right for a card room, where every object is the  same object. It is wrong for a wardrobe: a rail holds tees and jeans and  jackets, and drawing all of them as a tee would be a worse lie than the  flat quad it replaced, because a wrong SHAPE reads as information.    The renderer picks from `[itemModel, ...itemModelVariants]` using the  item's title — the same heuristic `silhouetteFor` already uses to choose a  garment outline, and for the same reason: the title is the only signal  present on every item, and a wrong guess costs a slightly odd shape rather  than the wrong item. `itemModel` is the fallback when nothing matches.    Deliberately NOT fuzzy-matched against a catalog — this picks a SHAPE, not  an identity. See docs/IDENTIFIER-FIRST.md for where that line sits.
 * @param gradedVariant A SEPARATE profile for graded/sealed copies of the same category.    This is not a flourish. Nobody puts a slabbed card in a binder — it does  not fit and it would be vandalism. Graded cards go on a wall, raw cards go  in pockets, and a space that ignores that is immediately wrong to the only  people who would use it. Same for CGC comics.
 */

data class GetSpatialPublicResponseProfile (

    /* Matches `crossly_market_categories.slug`, or '*' for the fallback. */
    @Json(name = "categorySlug")
    val categorySlug: kotlin.String,

    /* Room title, shown in the switcher. */
    @Json(name = "label")
    val label: kotlin.String,

    /* One line of why this space is shaped the way it is. Surfaced in the UI. */
    @Json(name = "tagline")
    val tagline: kotlin.String,

    @Json(name = "presentation")
    val presentation: GetSpatialPublicResponseProfile.Presentation,

    @Json(name = "itemSize")
    val itemSize: GetSpatialPublicResponseProfileItemSize,

    /* The container ladder, OUTERMOST FIRST. A binder holds pages, a page holds  cards. The solver walks this to decide what to create next when the  current container fills up. */
    @Json(name = "containers")
    val containers: kotlin.collections.List<GetSpatialPublicResponseProfileContainers>,

    /* Default grouping, in order. Each level becomes a divider or a container. */
    @Json(name = "defaultGroupBy")
    val defaultGroupBy: kotlin.collections.List<GetSpatialPublicResponseProfile.DefaultGroupBy>,

    /* Default ordering inside a group. */
    @Json(name = "defaultSortBy")
    val defaultSortBy: kotlin.collections.List<GetSpatialPublicResponseProfile.DefaultSortBy>,

    /* Other meshes items in this category may be drawn as, chosen PER ITEM.    One model per category is right for a card room, where every object is the  same object. It is wrong for a wardrobe: a rail holds tees and jeans and  jackets, and drawing all of them as a tee would be a worse lie than the  flat quad it replaced, because a wrong SHAPE reads as information.    The renderer picks from `[itemModel, ...itemModelVariants]` using the  item's title — the same heuristic `silhouetteFor` already uses to choose a  garment outline, and for the same reason: the title is the only signal  present on every item, and a wrong guess costs a slightly odd shape rather  than the wrong item. `itemModel` is the fallback when nothing matches.    Deliberately NOT fuzzy-matched against a catalog — this picks a SHAPE, not  an identity. See docs/IDENTIFIER-FIRST.md for where that line sits. */
    @Json(name = "itemModelVariants")
    val itemModelVariants: kotlin.collections.List<GetSpatialPublicResponseProfile.ItemModelVariants>? = null,

    /* A SEPARATE profile for graded/sealed copies of the same category.    This is not a flourish. Nobody puts a slabbed card in a binder — it does  not fit and it would be vandalism. Graded cards go on a wall, raw cards go  in pockets, and a space that ignores that is immediately wrong to the only  people who would use it. Same for CGC comics. */
    @Json(name = "gradedVariant")
    val gradedVariant: kotlin.String? = null

) {

    /**
     * 
     *
     * Values: flat,slabbed,boxed,cased,spined,hanging,standing
     */
    enum class Presentation(val value: kotlin.String) {
        @Json(name = "flat") flat("flat"),
        @Json(name = "slabbed") slabbed("slabbed"),
        @Json(name = "boxed") boxed("boxed"),
        @Json(name = "cased") cased("cased"),
        @Json(name = "spined") spined("spined"),
        @Json(name = "hanging") hanging("hanging"),
        @Json(name = "standing") standing("standing");
    }
    /**
     * Default grouping, in order. Each level becomes a divider or a container.
     *
     * Values: brand,condition,size,status,label,title,`value`,year,grade,set,franchise,game,team,era,genre,colour,age
     */
    enum class DefaultGroupBy(val value: kotlin.String) {
        @Json(name = "brand") brand("brand"),
        @Json(name = "condition") condition("condition"),
        @Json(name = "size") size("size"),
        @Json(name = "status") status("status"),
        @Json(name = "label") label("label"),
        @Json(name = "title") title("title"),
        @Json(name = "value") `value`("value"),
        @Json(name = "year") year("year"),
        @Json(name = "grade") grade("grade"),
        @Json(name = "set") set("set"),
        @Json(name = "franchise") franchise("franchise"),
        @Json(name = "game") game("game"),
        @Json(name = "team") team("team"),
        @Json(name = "era") era("era"),
        @Json(name = "genre") genre("genre"),
        @Json(name = "colour") colour("colour"),
        @Json(name = "age") age("age");
    }
    /**
     * Default ordering inside a group.
     *
     * Values: brand,condition,size,status,label,title,`value`,year,grade,set,franchise,game,team,era,genre,colour,age
     */
    enum class DefaultSortBy(val value: kotlin.String) {
        @Json(name = "brand") brand("brand"),
        @Json(name = "condition") condition("condition"),
        @Json(name = "size") size("size"),
        @Json(name = "status") status("status"),
        @Json(name = "label") label("label"),
        @Json(name = "title") title("title"),
        @Json(name = "value") `value`("value"),
        @Json(name = "year") year("year"),
        @Json(name = "grade") grade("grade"),
        @Json(name = "set") set("set"),
        @Json(name = "franchise") franchise("franchise"),
        @Json(name = "game") game("game"),
        @Json(name = "team") team("team"),
        @Json(name = "era") era("era"),
        @Json(name = "genre") genre("genre"),
        @Json(name = "colour") colour("colour"),
        @Json(name = "age") age("age");
    }
    /**
     * Other meshes items in this category may be drawn as, chosen PER ITEM.    One model per category is right for a card room, where every object is the  same object. It is wrong for a wardrobe: a rail holds tees and jeans and  jackets, and drawing all of them as a tee would be a worse lie than the  flat quad it replaced, because a wrong SHAPE reads as information.    The renderer picks from `[itemModel, ...itemModelVariants]` using the  item's title — the same heuristic `silhouetteFor` already uses to choose a  garment outline, and for the same reason: the title is the only signal  present on every item, and a wrong guess costs a slightly odd shape rather  than the wrong item. `itemModel` is the fallback when nothing matches.    Deliberately NOT fuzzy-matched against a catalog — this picks a SHAPE, not  an identity. See docs/IDENTIFIER-FIRST.md for where that line sits.
     *
     * Values: card,cardMinusSlab,comic,comicMinusSlab,vinyl,vinylMinusDisc,funkoMinusBox,legoMinusBox,minifig,gameMinusCase,book,sneaker,boot,handbag,watch,phone,laptop,tee,hoodie,jacket,jeans,dress,skirt
     */
    enum class ItemModelVariants(val value: kotlin.String) {
        @Json(name = "item-card") card("item-card"),
        @Json(name = "item-card-slab") cardMinusSlab("item-card-slab"),
        @Json(name = "item-comic") comic("item-comic"),
        @Json(name = "item-comic-slab") comicMinusSlab("item-comic-slab"),
        @Json(name = "item-vinyl") vinyl("item-vinyl"),
        @Json(name = "item-vinyl-disc") vinylMinusDisc("item-vinyl-disc"),
        @Json(name = "item-funko-box") funkoMinusBox("item-funko-box"),
        @Json(name = "item-lego-box") legoMinusBox("item-lego-box"),
        @Json(name = "item-minifig") minifig("item-minifig"),
        @Json(name = "item-game-case") gameMinusCase("item-game-case"),
        @Json(name = "item-book") book("item-book"),
        @Json(name = "item-sneaker") sneaker("item-sneaker"),
        @Json(name = "item-boot") boot("item-boot"),
        @Json(name = "item-handbag") handbag("item-handbag"),
        @Json(name = "item-watch") watch("item-watch"),
        @Json(name = "item-phone") phone("item-phone"),
        @Json(name = "item-laptop") laptop("item-laptop"),
        @Json(name = "item-tee") tee("item-tee"),
        @Json(name = "item-hoodie") hoodie("item-hoodie"),
        @Json(name = "item-jacket") jacket("item-jacket"),
        @Json(name = "item-jeans") jeans("item-jeans"),
        @Json(name = "item-dress") dress("item-dress"),
        @Json(name = "item-skirt") skirt("item-skirt");
    }
}

