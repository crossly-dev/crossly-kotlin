/**
 * Crossly Buyer API
 *
 * The buyer-facing Crossly API — cart, orders, wishlists, offers, cashback. A DIFFERENT principal from the seller API: authenticated by a buyer OAuth token scoped `buyer:*` and resolved by `resolveBuyer`. A seller Personal Access Token will NOT authenticate these endpoints.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package net.crossly.buyer.models

import net.crossly.buyer.models.CreateBuyerActivityResponseMatchOffer
import net.crossly.buyer.models.GetCatalogLookupResponseCatalog

import com.squareup.moshi.Json

/**
 * 
 *
 * @param verdict 
 * @param offer 
 * @param conditionComparable TRUE means the offer's condition and the page's are comparable. False means we matched the item but not its state — a used Crossly copy against a retailer's new one — and the UI must say so rather than claim a saving.
 * @param shippingKnown Always false today. Crossly shipping is computed at checkout from the buyer's address, which Scout does not have and should not send. Present so the surface that renders \"before shipping\" is reading a fact rather than hard-coding an assumption that stops being true when we add it.
 * @param alternates Other buyable offers for the SAME item, cheapest first, best excluded.  Deliberately not \"you might also like\". We have no behavioural data to build that from, and inventing it would put unrelated items under a badge whose entire value is that it only ever appears when we have the thing the buyer is actually looking at.  What these ARE is the same product from other sellers, in other conditions, at other prices — which is the choice a buyer on a product page genuinely wants, and the one a single \"cheapest\" result hides.
 * @param savingCents Positive when cheaper. Null when no page price was supplied.
 * @param catalog 
 */

data class CreateBuyerActivityResponseMatch (

    @Json(name = "verdict")
    val verdict: CreateBuyerActivityResponseMatch.Verdict,

    @Json(name = "offer")
    val offer: CreateBuyerActivityResponseMatchOffer,

    /* TRUE means the offer's condition and the page's are comparable. False means we matched the item but not its state — a used Crossly copy against a retailer's new one — and the UI must say so rather than claim a saving. */
    @Json(name = "conditionComparable")
    val conditionComparable: kotlin.Boolean,

    /* Always false today. Crossly shipping is computed at checkout from the buyer's address, which Scout does not have and should not send. Present so the surface that renders \"before shipping\" is reading a fact rather than hard-coding an assumption that stops being true when we add it. */
    @Json(name = "shippingKnown")
    val shippingKnown: kotlin.Boolean,

    /* Other buyable offers for the SAME item, cheapest first, best excluded.  Deliberately not \"you might also like\". We have no behavioural data to build that from, and inventing it would put unrelated items under a badge whose entire value is that it only ever appears when we have the thing the buyer is actually looking at.  What these ARE is the same product from other sellers, in other conditions, at other prices — which is the choice a buyer on a product page genuinely wants, and the one a single \"cheapest\" result hides. */
    @Json(name = "alternates")
    val alternates: kotlin.collections.List<CreateBuyerActivityResponseMatchOffer>,

    /* Positive when cheaper. Null when no page price was supplied. */
    @Json(name = "savingCents")
    val savingCents: java.math.BigDecimal? = null,

    @Json(name = "catalog")
    val catalog: GetCatalogLookupResponseCatalog? = null

) {

    /**
     * 
     *
     * Values: cheaper,notCheaper,noPagePrice,currencyMismatch
     */
    enum class Verdict(val value: kotlin.String) {
        @Json(name = "cheaper") cheaper("cheaper"),
        @Json(name = "not_cheaper") notCheaper("not_cheaper"),
        @Json(name = "no_page_price") noPagePrice("no_page_price"),
        @Json(name = "currency_mismatch") currencyMismatch("currency_mismatch");
    }
}

